//! HTTP client for AX endpoint fetching
//!
//! This module provides HTTP client functionality for fetching AX records.

/// HTTP client for AX endpoints
pub struct AxHttpClient;

impl AxHttpClient {
    /// Create a new HTTP client
    pub fn new() -> Self {
        todo!("Implementation will be added in task 6")
    }
}